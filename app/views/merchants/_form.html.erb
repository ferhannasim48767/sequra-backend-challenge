<%= form_with(model: merchant) do |form| %>
  <% if merchant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(merchant.errors.count, "error") %> prohibited this merchant from being saved:</h2>

      <ul>
        <% merchant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control", autocomplete:'off' %>
  </div>

  <div class="form-group">
    <%= form.label :email %>
    <%= form.text_field :email, class: "form-control", autocomplete:'off' %>
  </div>

  <div class="form-group">
    <%= form.label :CIF %>
    <%= form.text_field :cif, class: "form-control", autocomplete:'off' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>